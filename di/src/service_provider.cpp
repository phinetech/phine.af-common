/**
 * @file service_provider.cpp
 * @brief Implementation of the ServiceProvider interface
 * 
 * This file would normally contain the implementation of the ServiceProvider interface.
 * However, since ServiceProvider is a pure interface with only template methods that are
 * implemented in the header, this file is essentially empty except for the namespace
 * declarations.
 * 
 * It's included in the build system for consistency and to allow for future
 * non-template method implementations if needed.
 */

 #include "service_provider.h"

 namespace af {
 namespace di {
 
 // Service provider implementation is entirely in the header since it's a template-based interface
 
 } // namespace di
 } // namespace af